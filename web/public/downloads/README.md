# APK bu yerga qo'ying

Android ilovasini (`ipak-yoli.app`) build qilib, APK faylini shu papkaga
`ipak-yoli.apk` nomi bilan joylang. Sayt `/download/android` orqali beradi.

Fayl nomini `.env` dagi `ANDROID_APK_FILE` orqali o'zgartirish mumkin.
